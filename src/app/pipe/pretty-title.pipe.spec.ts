import { PrettyTitlePipe } from './pretty-title.pipe';

describe('PrettyTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
